info:
  name: Update User
  type: http
  seq: 1

http:
  method: PUT
  url: "{{host}}/users/1"
  body:
    type: json
    data: |-
      {
        "name":"Alice B."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
