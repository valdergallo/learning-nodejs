info:
  name: Create User
  type: http
  seq: 1

http:
  method: POST
  url: "{{host}}/users"
  body:
    type: json
    data: |-
      {
        "name":"Max",
        "email":"max@x.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
